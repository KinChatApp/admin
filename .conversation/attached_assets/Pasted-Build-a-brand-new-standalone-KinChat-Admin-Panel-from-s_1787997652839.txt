Build a brand-new standalone KinChat Admin Panel from scratch.

This is a completely new project. Do not look for or depend on any existing KinChat website or application code.

The goal of this task is to create the initial Admin Panel UI foundation only, so I can run the project and preview it at the end.

Do not implement backend functionality yet.

---

1. Tech Stack

Use:

- Next.js
- TypeScript
- React
- Tailwind CSS
- shadcn/ui where useful
- Lucide icons

Use the latest stable versions that work well together.

Initialize the project properly with a clean, maintainable structure.

---

2. Project Goal

Create a professional Admin Panel for a future messaging platform called KinChat.

This first version should only establish:

- visual identity
- admin layout
- sidebar
- header
- navigation
- responsive behavior
- basic Overview page
- placeholder pages for future sections

Do NOT build real admin functionality.

---

3. KinChat Admin Theme

Create a completely new visual system specifically for KinChat.

Use this exact palette:

Primary Brand Blue: #2563EB
Primary Dark Blue: #1D4ED8

Background: #07111F
Surface / Cards: #0D1B2A
Elevated Surface: #12243A
Border: #1E3A5F

Primary Text: #F8FAFC
Secondary Text: #94A3B8
Muted Text: #64748B

Success: #22C55E
Error: #EF4444

Do not randomly introduce another primary color.

The overall visual feeling should be:

- deep navy
- modern
- premium
- clean
- technical
- professional
- restrained
- high contrast
- suitable for an enterprise messaging platform

Avoid the typical generic AI-generated dashboard appearance.

Avoid excessive gradients, glassmorphism, huge rounded cards, excessive shadows, and unnecessary animations.

---

4. Typography

Use:

Space Grotesk as the primary font.

Use:

JetBrains Mono for technical/data-oriented elements where appropriate.

Create a consistent typography hierarchy for:

- page titles
- section titles
- body text
- labels
- metadata
- numbers

---

5. Main Admin Layout

Create:

┌──────────────────────────────────────────────┐
│                 Top Header                   │
├──────────────┬───────────────────────────────┤
│              │                               │
│   Sidebar    │        Main Content           │
│              │                               │
│              │                               │
└──────────────┴───────────────────────────────┘

Sidebar

Create a professional collapsible sidebar.

It should contain:

KinChat
Admin

Overview

Identity & Users
Communication
Calls
Content & Media
Trust & Safety
Engagement
Analytics
Platform
Operations
Security
Developer

Use appropriate Lucide icons.

Include:

- active state
- hover state
- collapsed state
- smooth but subtle interaction

Do not add unnecessary menu items.

---

6. Header

Create a clean top header containing:

- page/breadcrumb context
- global search UI
- notification icon
- admin profile/avatar
- profile dropdown

The header should visually integrate with the sidebar and main content.

---

7. Routes

Create the following basic routes:

/admin
/admin/users
/admin/communication
/admin/calls
/admin/content
/admin/safety
/admin/engagement
/admin/analytics
/admin/platform
/admin/operations
/admin/security
/admin/developer

For now, only "/admin" needs a meaningful dashboard.

All other routes should have clean placeholder pages showing:

- page title
- short description
- "Coming soon"

Do not implement functionality inside these sections.

---

8. Overview Page

Create a polished but simple Overview page.

Include:

Header

Overview
Monitor the current state of KinChat.

Stat cards

Create 4–5 realistic placeholder statistics such as:

Total Users
Active Users
Messages Today
Online Users
Pending Reports

These are ONLY visual demo values.

Clearly structure the data so it can later be replaced by real API data.

Additional sections

Add:

- Recent Activity placeholder
- System Status placeholder

Keep the page clean.

Do not build complex charts yet.

---

9. Responsive Design

The Admin Panel must work properly on:

- desktop
- tablet
- mobile

Desktop:

Sidebar + Header + Content

Mobile:

Header
Drawer Navigation
Content

Do not simply shrink the desktop layout.

Tables/components should remain usable on small screens.

---

10. Component Architecture

Create reusable components for the UI shell.

For example:

components/
├── admin/
│   ├── AdminSidebar
│   ├── AdminHeader
│   ├── AdminLayout
│   ├── StatCard
│   ├── PageHeader
│   └── PlaceholderPage

Use your own judgment if the existing Next.js conventions suggest a better structure.

The important thing is to keep the UI modular.

---

11. Future-Proof Navigation

The current top-level navigation must remain stable.

Future features should be added as child modules inside existing domains.

For example:

Communication
├── Private Chats
├── Groups
├── Channels
├── Communities
└── Stories

But DO NOT implement these future modules now.

Only make sure the architecture can support them later.

---

12. Do NOT Implement Yet

Absolutely do not implement:

- authentication
- database
- Supabase
- API
- user management logic
- moderation logic
- reports system
- notification system
- real analytics
- permissions
- audit logs
- real admin actions
- backend integration

This task is UI foundation only.

---

13. Quality Requirements

The final UI should NOT look like a template copied from a random dashboard.

It should feel like a real product that could eventually become the official internal administration system for KinChat.

Pay particular attention to:

- spacing
- alignment
- typography
- hierarchy
- sidebar proportions
- card density
- hover states
- active states
- responsive behavior
- visual consistency
- accessibility
- contrast

Keep the design sophisticated but not over-designed.

---

14. Agent Execution

Work directly on the project.

Do not only explain the code or provide instructions.

Actually:

1. Initialize the project.
2. Install required dependencies.
3. Create the project structure.
4. Write the code.
5. Start the development server.
6. Verify that "/admin" works.
7. Verify that the navigation routes work.
8. Fix any TypeScript/build/runtime errors.
9. Leave the development server running so I can preview the result.

If the environment provides a browser/preview mechanism, use it to verify the UI.

Do not stop after generating files.

The final state must be a working previewable Admin Panel UI.

---

15. Final Response

After completing the implementation, report only:

- project created
- tech stack used
- main routes
- major UI components
- how to open the preview
- whether the build/runtime check passed

Do not describe future features that were not implemented.

Most importantly:

Build the project now. Do not just give me a tutorial.